# update

declare -a rootfiles=(
    ".bashrc"
    ".bash_profile"
    ".inputrc"
    "_vimrc"
    "_gvimrc"
    "bash/"
)

for f in "${rootfiles[@]}"; do
    cp -r ~/$f ./src/
done

rm -f ./src/bash/ualias
rm -f ./src/bash/ufunc
rm -f ./src/bash/upath

unset rootfiles
