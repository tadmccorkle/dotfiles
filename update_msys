# update_msys

declare -a rootfiles=(
    ".bashrc"
    ".bash_profile"
    ".inputrc"
    "_vimrc"
    "_gvimrc"
    "bash/"
)

for f in "${rootfiles[@]}"; do
    cp -r ~/$f ./msys2/
done

rm ./msys2/bash/ualias

unset rootfiles
